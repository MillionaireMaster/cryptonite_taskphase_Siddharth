# Reading Manuals

`man` is short for manual and will display the manual of the command you pass as an argument.
The arguments to the `man` command aren't file paths, but just the names of the entries themselves.
In this challenge we have to type `man challenge` and then type `/challenge/challenge --svsekh NUM` to get the flag, The NUM will be mentioned in the manual.
